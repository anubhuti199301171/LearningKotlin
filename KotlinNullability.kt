fun main()
{
    var myName : String? = null;//if we add ? in front of a datatype it becomes null type
    
    if(myName!=null)
    	print(myName);
    
    /*
     * Output-->
     * Nothing gets printed
     * */
}